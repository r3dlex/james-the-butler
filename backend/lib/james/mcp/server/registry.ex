defmodule James.MCP.Server.Registry do
  @moduledoc "Registry for looking up MCP server GenServer processes by server id."
  # This registry is started via Application children (Registry module)
  # See James.Application for the child specification.
end
